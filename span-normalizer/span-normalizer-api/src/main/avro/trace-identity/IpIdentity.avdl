@namespace("org.hypertrace.core.spannormalizer")
protocol IpIdentityProtocol {
  record IpIdentity {
    string tenant_id;
    string environment;
    string host_addr;
    string peer_addr;
    string peer_port;
  }
}
